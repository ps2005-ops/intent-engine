anthropic==0.116.0
pydantic==2.13.4
python-dotenv==1.2.1
scikit-learn==1.6.1
google-auth==2.50.0
google-auth-oauthlib==1.3.1
google-api-python-client==2.198.0
Pillow==11.3.0
faster-whisper==1.2.1
requests==2.32.5
pypdf==5.1.0
# Hosted-runtime Postgres backend. Optional at runtime (storage.durable falls
# back to sqlite3 and imports this lazily), pinned here so CI resolves the same
# driver every time. Mirrors the [postgres] extra in pyproject.toml.
psycopg[binary]==3.2.3
