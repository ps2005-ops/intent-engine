# Demo proof — what a reader can be shown

*Generated from `reports/asi25_state.json` at `712613b7588f`. Every total on this page is computed; none is typed.*

## The five strongest live proofs

1. **150/150 primary questions answered semantically**, with 0 cross-company contaminations and 25/25 contextual follow-ups staying inside their own run.
2. **UNEXPLAINED_COLLAPSES = 0** and **0 near-identical pairs between two non-abstaining companies**, under the stricter of two instruments.
3. **22/25 companies abstained**, each with a stated reason, an information priority and a route back — the product declining to read a record it could not read.
4. **UI matrix over 2150 frames** (233170 elements, 5 widths x 2 themes, no sampling) with 0 overflow and 0 contrast failures.
5. **Performance inside every target on first attempt**: CORE p50 25.1s, p90 46.6s, max 61.7s against 60/100/120.

## Companies that did not abstain

| company | final class | grounding | decision force | independent origins |
|---|---|---|---|---|
| Procore | PRODUCT_DEFECT | NAME_ONLY | CLASS_PRIOR_REINFORCED_BY_EVIDENCE | 0 |
| Motive | DECISION_GRADE_READING | NAME_ONLY | CLASS_PRIOR_ONLY | 1 |
| Verkada | DECISION_GRADE_READING | NAME_ONLY | CLASS_PRIOR_ONLY | 1 |

`DECISION_GRADE_READING` is assigned by the qualification harness
from `independent_origins > 0` alone. It does **not** read the
grounding verdict, so a company can carry the label while the
product's own page says the reading is grounded on the name only.
Reported here as measured, and recorded as a finding rather than
corrected inside a freeze.
