# PLAN — 2026-07-21 (intent-engine) — for Fable 5

*Execution queue for tomorrow, written 2026-07-20. Same house style as
`ROADMAP.md`: every task has a concrete definition-of-done (a test or
check that returns pass/fail). Three workstreams: **A** finish the live
tests, **B** fix loose ends + automate, **C** build Marketing v2
(`marketing/MARKETING_PLAN_V2.md`). Job-application-agent has its own
plan at `~/job-application-agent/PLAN_2026-07-21.md`.*

## Walls that bind every task here (do not cross)

1. **Live model calls cost money and are founder-gated.** The synthetic
   eval `--live` leg (~$1.78, ≤100 calls) and any real API call run **on
   the Mac with explicit founder go** — not from the sandbox, which must
   make **0 model calls** (standing wall).
2. **No accuracy claims** enter any report/marketing asset until ≥30
   resolved predictions per source + founder calibration review (A-M5).
3. **Prompts / enum / mechanism library stay frozen.** Marketing work
   never edits engine `src/` generation prompts.
4. **Append-only** everywhere: ledgers and run histories gain rows, never
   mutate.
5. Commit only with the offline suite green and **EXIT=0 explicitly
   checked** (repo convention).

---

## Workstream A — run & complete the live tests

### A1 — Two more T009 control-world stability runs
- **Status**: RUNNABLE (founder-executed on Mac; Fable 5 prepares +
  harvests).
- **Why**: control-world hallucination rate went 3/8 → 8/8 → 8/8 across
  three runs of a *non-deterministic* leg. Recall is replicated at 1.000,
  but one metric (control cleanliness) needs ≥2 more points to bound its
  variance. This was the founder's own open question.
- **Command** (Mac): `python scripts/run_synthetic_world_eval.py --live`
  — run twice. Each auto-appends a row to
  `reports/synthetic_worlds_run_history.jsonl` and archives a per-run
  JSON to `reports/synthetic_worlds_runs/`.
- **Definition of done**: run-history file gains 2 new v1.1 rows (5 total);
  Fable 5 updates `reports/synthetic_worlds_eval_live.md`'s cross-run
  table and computes the control-clean rate + its spread across all 5
  runs; the stability question in `MORNING_HANDOFF.md` is either closed
  (if 8/8 holds) or re-stated with the observed variance. No prompt/enum
  edits. Cost logged.
- **Walls**: founder-run; sandbox stays at 0 calls.

### A2 — Verify live ledger accrual actually started
- **Status**: RUNNABLE.
- **Why**: market/baseline predictions accrue from the cron's first fire
  (Monday 18:30 ET). Today *is* that Monday; the handoff couldn't read the
  crontab from the sandbox.
- **Definition of done**: confirm the Monday spend-log row exists and that
  `prediction_ledger` gained market/baseline rows on schedule (direct DB
  read); if the cron never fired, install the line from
  `cron_lines_to_install.txt` and record that it was pending. Report the
  ledger count by source (currently 12 total, 0 resolved).
- **Walls**: display-only; no tuning.

### A3 — Fold results into the handoff + roadmap
- **Status**: RUNNABLE (blocked by A1, A2).
- **Definition of done**: `MORNING_HANDOFF.md` + `ROADMAP.md` (T009) updated
  with the 5-run stability finding and the verified ledger state; committed
  with suite green + EXIT=0.

---

## Workstream B — fix loose ends & automate

### B1 — Resolve the uncommitted git state (the real loose end)
- **Status**: RUNNABLE — **do this first tomorrow.**
- **Why**: a staged `git rm --cached` on the synthetic-worlds files
  (`synthetic_worlds.py`, `run_synthetic_world_eval.py`, both tests, the
  whole `reports/synthetic_worlds_*` tree) is sitting **uncommitted** —
  they show deleted-from-index + untracked at once. Separately, real edits
  across `daily_market_predictions.py`, `daily_prediction_policy.py`,
  `headline_feed.py`, `simulator/cli.py`, `simulator/pipeline.py`, docs,
  and 3 tests are uncommitted, and run 3's outputs are untracked. Not
  corruption — unfinished housekeeping (confirmed via diff last session).
- **Definition of done**: decide per file group whether the `rm --cached`
  was intended (almost certainly **not** — these files belong in git);
  restage/keep them tracked, commit the pipeline edits + the 3rd-run
  outputs, and end at a **clean `git status`** with the offline suite green
  and EXIT=0. No history rewrite.
- **Walls**: standard commit discipline.

### B2 — Add a pre-commit / CI guard so this can't recur
- **Status**: RUNNABLE.
- **Definition of done**: a `citation_check.sh`-style guard (or a git
  pre-commit hook) that runs `.venv/bin/python -m pytest -q`, asserts
  EXIT=0, and fails the commit on a dirty/again-untracked
  `reports/synthetic_worlds_*` tree. One test proves the hook triggers.
- **Walls**: offline only; 0 model calls.

---

## Workstream C — build Marketing v2 (`marketing/MARKETING_PLAN_V2.md`)

*Governing principle: **automate generation, gate publication and
claims.** Every task below emits drafts into an approval queue. Nothing
posts/sends without per-item approval + `PUBLISHING_ENABLED`. Priority
order = the plan's four priorities. C1–C2 are day-1 RUNNABLE; C3–C8 are
the sequenced backlog to mid-August, each with a DoD so they're runnable
when reached.*

### C1 — Content-engine skeleton (Priority 2, start day 1)
- **Status**: RUNNABLE.
- **Build**: `marketing/content_engine/render.py` — a normalized
  `ContentSource` (from `core/regime_report.py` / `prediction_ledger.py` /
  `scripts/render_founder_report.py`) and pure functions
  `source -> {asset_type: draft}` for at least: website article, LinkedIn,
  X thread, newsletter, founder email. Each draft carries the T:1–T:6
  trace table and lands in `marketing/content_engine/drafts/<date>/`.
- **Definition of done**: a test feeds the **real 2026-07-17 run** object
  and asserts (a) ≥5 asset types produced, (b) every asset contains the
  trace table, (c) **zero** predictive-accuracy strings (reuse the
  outreach claim audit), (d) zero network calls. No fact appears in an
  asset that isn't in the source object.
- **Walls**: generation only; drafts never auto-publish.

### C2 — Productize the founder report to PDF (Priority 1, start day 1)
- **Status**: RUNNABLE.
- **Build**: extend `scripts/render_founder_report.py` to emit the section
  set — Executive Summary → Decision → Mechanisms → Evidence →
  Contradictions → Scenario tree → Metrics to watch → 90-day checklist →
  Prediction — and render to PDF (repo `pdf` skill / headless renderer),
  house style per `docs/report_mockup/`.
- **Definition of done**: given a real analysis fixture, a PDF is produced
  with all sections; the "what we could not verify" block renders even when
  empty-labelled; honesty markers present; no accuracy claim. Test asserts
  section presence + marker presence.

### C3 — Ledger→content event hook (Priority 4)
- **Status**: NEXT.
- **Build**: post-append hook/watcher on `prediction_ledger.py` → calls C1
  to fan a new prediction into {markdown page, SEO page, newsletter,
  LinkedIn, X, founder summary, GitHub example, analytics event} as drafts.
- **Definition of done**: appending a test prediction produces the full
  draft set in the queue and **zero** publishes; append-only ledger
  untouched; idempotent (re-running doesn't double-draft).

### C4 — Feedback loop
- **Status**: NEXT.
- **Build**: `marketing/feedback/` append-only `feedback.jsonl` (useful 1–5
  / what was wrong / what surprised you / would you pay / can we quote);
  metrics computed by code.
- **Definition of done**: submitting a response appends exactly one row;
  "quote = yes" is the only testimonial-eligible path and still needs
  founder approval before use. Test covers append-only + the quote gate.

### C5 — Lightweight CRM (Priority 3)
- **Status**: NEXT.
- **Build**: `marketing/crm/` append-only `crm.jsonl` keyed by
  `prospect_id`, lifecycle prospect→…→advocate + the waitlist funnel;
  every interaction writes a row; metrics by code. Built on
  `outreach/tracking_ledger_schema.md`.
- **Definition of done**: a scripted lifecycle writes the right state rows;
  reads collapse to latest per id; no `sent` row without a prior `approved`
  + non-null `approved_by`; conversion metrics computed, not stored.

### C6 — Commit-triggered content
- **Status**: NEXT.
- **Build**: `marketing/content_engine/from_commits.py` + a post-commit /
  nightly `git log` walker → changelog + LinkedIn + X drafts to the queue.
- **Definition of done**: a fixture commit range yields a changelog entry +
  social drafts, all trace-audited, none published.

### C7 — Public pages as SEO assets
- **Status**: NEXT.
- **Build**: generators for Predictions (live ledger view), Leaderboard (vs
  baselines, honest), Mechanism Library, Case Studies, Research, Changelog.
- **Definition of done**: each page renders from real data; the Predictions
  and Leaderboard pages show raw rows + "too few resolved to claim
  calibration" until the gate clears — asserted by test.

### C8 — Public roadmap page
- **Status**: NEXT.
- **Build**: auto-generate Done/In-Progress/Testing/Next/Ideas from
  `ROADMAP.md` + task queue.
- **Definition of done**: page regenerates from the source docs; no manual
  duplication; drafts to queue.

---

## Definition of done for the day

- 5 total T009 live runs; control-stability question resolved or quantified
  (A1–A3).
- `git status` clean; recurrence guard in place (B1–B2).
- Content engine + productized PDF report exist and are tested (C1–C2);
  C3–C8 remain as a sequenced, DoD-ready backlog to mid-August.
- Every marketing artifact is a draft in an approval queue; **nothing
  published, no accuracy claim made.**
